# Holds real credentials (API keys, endpoints) — never commit this. Copy
# config.yaml.temp to config.yaml and fill in your own values instead.
config.yaml

# Downloaded Allure CLI tool and generated report/results — reproducible artifacts
.tools/
allure-report/
allure-results/

# Python bytecode cache
__pycache__/
*.pyc
